import React, {useEffect} from 'react';

const Location = () => {

    useEffect(() => {
        document.title = "Location";
    })

    return (
        <div>
            Location
        </div>
    );
};

export default Location;
